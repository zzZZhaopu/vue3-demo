# 测试环境变量
# 环境标识
NODE_ENV=production
VITE_ENV=test

# 是否启用 SourceMap (方便调试测试问题)
VITE_BUILD_SOURCEMAP=true

# 是否移除 console.log (保留用于测试调试)
VITE_DROP_CONSOLE=false

# API 地址 (测试服务器)
VITE_API_BASE_URL=https://test-api.example.com/api
